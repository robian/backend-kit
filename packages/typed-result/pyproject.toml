[project]
name = "typed-result"
version = "0.1.0"
description = "A small, strictly typed Result type for Python"
readme = "README.md"
requires-python = ">=3.14"
authors = [
    { name = "robian", email = "288016+robian@users.noreply.github.com" },
]
dependencies = []

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "typed_result"
